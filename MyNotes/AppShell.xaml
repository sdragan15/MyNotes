<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MyNotes.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MyNotes.View"
    Shell.FlyoutBehavior="Flyout"
    Title="MyNotes">

    <Shell.FlyoutHeader>
        <Grid Padding="12" BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}">
            <Label Text="MyNotes" FontAttributes="Bold" FontSize="20"/>
        </Grid>
    </Shell.FlyoutHeader>

    <FlyoutItem Title="Todos" Icon="todo.svg">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Icon="notes.svg" Title="Notes">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

</Shell>
